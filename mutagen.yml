sync:
  defaults:
    ignore:
      vcs: false
      paths:
        - .DS_Store # macOS files
        - .idea
    permissions:
      defaultFileMode: 644
      defaultDirectoryMode: 755
  circuitbreakerbundle:
    alpha: "."
    beta: "docker://root@circuit-breaker-bundle-php7/srv/www"
